# Backend origin for Next.js rewrites (browser calls same-origin /api/v1 → proxied here)
API_UPSTREAM=https://viah.aidaki.ai

# App URL for any server-side fetch (must match dev server, e.g. port 3003)
# NEXT_PUBLIC_APP_URL=http://localhost:3003

# Optional UUIDs for student signup form defaults
# NEXT_PUBLIC_SIGNUP_LEVEL_ID=
# NEXT_PUBLIC_SIGNUP_SPECIALITY_ID=
